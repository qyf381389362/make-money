# Project Backlog & Todos — 待办任务与路线图

本文档用于记录 Make Money 项目当前的版本路线图和未来待开发的高级任务（Backlog）。任务按开发阶段分类，便于开发者快速认领。

---

## 📅 版本路线图 (Roadmap)

### 🟢 v1a — 基础持仓与决策日记 (已发布 ✅)
*目标：建立本地存量记账、日均收盘行情自动刷新和操作决策记录的基础闭环。*
- [x] **T1** — 增加买入均价计算除零保护，防范参数错误导致的进程崩溃。
- [x] **T2** — 为加权平均成本核算、清仓物理删除及超卖保护编写完整的 `pytest` 单元测试。
- [x] **T3** — 在刷新价格网络请求期间，前端增加刷新按钮置灰及 `PortfolioTable` 加载遮罩与骨架屏。
- [x] **T4** — 优化持仓列表行情快照的 SQL 连接查询，利用窗口函数（Window Function）消除 N+1 查询隐患。
- [x] **QA-01** — 修复部分持仓缺乏收盘行情价格时，资产盈亏全显 `--` 的问题（改为均价兜底）。
- [x] **QA-02** — 修复重复刷新价格时由于复合唯一索引引发的 `IntegrityError` 并发回滚崩溃。
- [x] **QA-03** — 修复卖出清仓后持仓记录残留的问题（改为数据库中彻底物理删除）。

---

### 🟢 v1b — AI 深度决策复盘与基金净值获取 (已发布 ✅，PR #2)
*目标：引入本地/云端 LLM 分析能力，并扩展行情渠道以支持多类理财资产。实现已合并至 commit `7e8caaf`。*

#### 1. AI 决策动机问责审计 (AI Audit)
- [x] **T5 (P1)** — 接入 Gemini API（`gemini-2.5-flash`），BackgroundTasks 异步根据“买入/卖出原因”进行投资心理分类（追涨杀跌/贪婪/恐慌/理性分析/其它），缓存 `motivation_type`+`ai_audit`。
- [x] **T6 (P2)** — AI 投资审计看板 `AiMetrics.tsx`：心理健康分、性格雷达、偏误盈亏榜、累计盈亏曲线（recharts）。

#### 2. 公募基金行情适配 (Fund NAV)
- [x] **T7 (P1)** — 价格服务对 6 位 `fund` 代码分流天天基金接口拉取单位净值并持久化快照。
- [x] **T8 (P2)** — 前端 `PortfolioTable` 展示场外基金与场内 ETF 的混合损益。

#### 3. UX 优化
- [x] **T9 (P3)** — 日记原因模糊搜索与筛选。

---

### 🔴 v2.0 — 云同步、资产漏斗与自动化导入 (远期规划 🚀)
*目标：从单机走向云端，实现多终端同步、多账户管理和便捷的交割单自动解析。*

- [ ] **T10 (P2)** — **安全认证体系**：引入基于 JWT / OAuth2 的用户账户注册登录，支持多租户数据完全隔离。
- [ ] **T11 (P1)** — **券商交割单自动导入**：支持上传主流券商（如华泰、招商、同花顺等）导出的 CSV 交割单，一键自动解析成 journal_entries 流水并一键初始化持仓。（✅ 已 /autoplan 工程+设计深审，实现方案见 `PLAN.md`；MVP = `apply_trade` 重构 + `external_id` 去重 + 1 适配器 + 通用列映射 + 预览/确认两步）
- [ ] **T12 (P2)** — **大类资产漏斗图**：前端引入可视化库（ECharts/Recharts），向用户展示大类资产（A股、公募基金、场内ETF、现金）的资金占比与配置建议。
- [ ] **T13 (P3)** — **周度/月度损益变化曲线**：增加历史总资产市值变化曲线图，直观展示个人财富的成长轨迹。
  > 注：T12/T13 的「累计盈亏曲线、心理偏误图」大部分已由 v1b 的 `AiMetrics.tsx` 覆盖；真正净新增为个股 B/S 标记图与大类资产漏斗图。

#### T11 深审推迟项 (Deferred from /autoplan，超出 MVP)
- [ ] **T14 (P3)** — 公司行为自动入账（分红/送股/拆合股）。
- [ ] **T15 (P3)** — 多券商 × 多账户合并导入与去重。
- [ ] **T16 (P3)** — 交割单费用（手续费/印花税/过户费）精确税费明细分摊。
- [ ] **T17 (P4)** — PDF/图片对账单 OCR 解析。
- [ ] **T18 (P4)** — 交割单增量自动同步。
